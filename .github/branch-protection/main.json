{
  "branch": "main",
  "owner_access": "owner can always administer and push intentionally",
  "required_status_checks": [],
  "manual_validation_checks": [
    "Fast validation (ubuntu)",
    "Fast validation (macos)",
    "Runtime smoke (ubuntu)",
    "Runtime smoke (macos)",
    "Release dry-run",
    "MCP runtime pin freshness",
    "MCP safe call smoke (ubuntu)",
    "No-paid static security"
  ],
  "allow_force_pushes": false,
  "allow_deletions": false,
  "require_linear_history": true,
  "required_pull_request_reviews": {
    "required_approving_review_count": 0,
    "dismiss_stale_reviews": false,
    "require_code_owner_review": false
  },
  "enforce_admins": false,
  "notes": [
    "This file is desired state documentation, not an auto-applied policy.",
    "The owner keeps direct access to main for controlled personal workflow.",
    "GitHub Actions are manual-only by owner policy; run validation workflows only when a task explicitly asks for CI.",
    "manual_validation_checks names must match current workflow job names for human and ruleset reference."
  ]
}
