interface:
  display_name: "Compatibility Browser Task"
  short_description: "Старый trigger в managed browser route"
  default_prompt: "Используй $webwright-task для managed CloakBrowser workflow."
dependencies:
  tools: []
policy:
  allow_implicit_invocation: true
