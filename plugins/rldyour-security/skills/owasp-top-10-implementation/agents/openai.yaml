interface:
  display_name: OWASP Implementation
  short_description: 'Русскоязычная Codex skill metadata. EN: Auto OWASP secure implementation'
  default_prompt: 'Используй $OWASP Implementation для этой задачи с русскоязычным контекстом владельца. EN: Use $owasp-top-10-implementation to add non-blocking OWASP security guidance while implementing security-relevant changes.'
dependencies:
  tools:
  - type: mcp
    value: serena
    description: 'Serena MCP: семантический анализ кода, memories, symbols, references и точечные правки. EN: semantic code inspection, memories, symbols, references, and precise edits.'
  - type: mcp
    value: semgrep
    description: 'Semgrep: статический security scan, когда он применим; findings требуют ручной проверки. EN: static security scanning when applicable; findings still require manual validation.'
policy:
  allow_implicit_invocation: true
