interface:
  display_name: "OWASP Implementation"
  short_description: "Auto OWASP secure implementation"
  default_prompt: "Use $owasp-top-10-implementation to add non-blocking OWASP security guidance while implementing security-relevant changes."

dependencies:
  tools:
    - type: "mcp"
      value: "serena"
      description: "Preferred for precise code inspection and source-to-sink tracing when available."
    - type: "mcp"
      value: "semgrep"
      description: "Optional SAST support when a task needs automated security checks."

policy:
  allow_implicit_invocation: true
