interface:
  display_name: Flow Post-Task Sync
  short_description: 'Финальный sync. EN: post-task sync'
  default_prompt: 'Используй $flow-post-task-sync: memory, docs, git, fullrepo. EN: final sync.'
dependencies:
  tools:
  - type: mcp
    value: serena
    description: 'Семантика Serena. EN: code symbols, refs, memory.'
policy:
  allow_implicit_invocation: true
