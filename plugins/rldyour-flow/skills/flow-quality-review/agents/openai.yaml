interface:
  display_name: "Flow Quality Review"
  short_description: "Review correctness and tech debt"
  default_prompt: "Use $flow-quality-review to find hacks, shortcuts, edge cases, and quality issues."

dependencies:
  tools:
    - type: "mcp"
      value: "serena"
      description: "Targeted symbol body inspection for quality review."

policy:
  allow_implicit_invocation: true

