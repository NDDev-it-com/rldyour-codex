interface:
  display_name: Instruction Docs Sync
  short_description: 'Русскоязычная Codex skill metadata. EN: Sync AGENTS.md and Claude memory'
  default_prompt: 'Используй $Instruction Docs Sync для этой задачи с русскоязычным контекстом владельца. EN: Use $instruction-docs-sync to update AGENTS.md and .claude/CLAUDE.md from verified project facts.'
dependencies:
  tools:
  - type: mcp
    value: serena
    description: 'Serena MCP: семантический анализ кода, memories, symbols, references и точечные правки. EN: semantic code inspection, memories, symbols, references, and precise edits.'
policy:
  allow_implicit_invocation: true
