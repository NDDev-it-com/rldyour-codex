interface:
  display_name: Instruction Docs Sync
  short_description: 'Синхронизация docs. EN: instruction docs'
  default_prompt: 'Используй $instruction-docs-sync: синхронизируй AGENTS/CLAUDE. EN: docs sync.'
dependencies:
  tools:
  - type: mcp
    value: serena
    description: 'Семантика Serena. EN: code symbols, refs, memory.'
policy:
  allow_implicit_invocation: true
