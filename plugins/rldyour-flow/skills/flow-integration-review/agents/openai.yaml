interface:
  display_name: Flow Integration Review
  short_description: 'Ревью integration. EN: sync boundaries'
  default_prompt: 'Используй $flow-integration-review: проверь интеграции. EN: integration review.'
dependencies:
  tools:
  - type: mcp
    value: serena
    description: 'Семантика Serena. EN: code symbols, refs, memory.'
policy:
  allow_implicit_invocation: false
