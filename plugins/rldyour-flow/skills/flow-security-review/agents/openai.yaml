interface:
  display_name: Flow Security Review
  short_description: 'Ревью security. EN: security check'
  default_prompt: 'Используй $flow-security-review: проверь security. EN: security review.'
dependencies:
  tools:
  - type: mcp
    value: serena
    description: 'Семантика Serena. EN: code symbols, refs, memory.'
policy:
  allow_implicit_invocation: false
