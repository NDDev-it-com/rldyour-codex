interface:
  display_name: Flow Architecture Review
  short_description: 'Русскоязычная Codex skill metadata. EN: Review architecture boundaries'
  default_prompt: 'Используй $Flow Architecture Review для этой задачи с русскоязычным контекстом владельца. EN: Use $flow-architecture-review to check architecture, dependencies, layers, and data flow.'
dependencies:
  tools:
  - type: mcp
    value: serena
    description: 'Serena MCP: семантический анализ кода, memories, symbols, references и точечные правки. EN: semantic code inspection, memories, symbols, references, and precise edits.'
policy:
  allow_implicit_invocation: false
