interface:
  display_name: LSP Setup
  short_description: 'Настройка LSP. EN: language server setup'
  default_prompt: 'Используй $lsp-setup: установи/почини LSP. EN: setup language server.'
dependencies:
  tools:
  - type: mcp
    value: serena
    description: 'Семантика Serena. EN: code symbols, refs, memory.'
policy:
  allow_implicit_invocation: true
