interface:
  display_name: LSP Routing
  short_description: 'Маршрут LSP. EN: language-server routing'
  default_prompt: 'Используй $lsp-routing: выбери LSP flow. EN: route language tools.'
dependencies:
  tools:
  - type: mcp
    value: serena
    description: 'Семантика Serena. EN: code symbols, refs, memory.'
policy:
  allow_implicit_invocation: true
