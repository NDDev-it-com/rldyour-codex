interface:
  display_name: LSP Health Check
  short_description: 'Русскоязычная Codex skill metadata. EN: Verify LSP commands and project prerequisites'
  default_prompt: 'Используй $LSP Health Check для этой задачи с русскоязычным контекстом владельца. EN: Use $lsp-health-check to verify local language servers and project prerequisites.'
dependencies:
  tools:
  - type: mcp
    value: serena
    description: 'Serena MCP: семантический анализ кода, memories, symbols, references и точечные правки. EN: semantic code inspection, memories, symbols, references, and precise edits.'
policy:
  allow_implicit_invocation: true
