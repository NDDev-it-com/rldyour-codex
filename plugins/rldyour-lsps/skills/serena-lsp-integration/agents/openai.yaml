interface:
  display_name: Serena LSP Integration
  short_description: 'Связка Serena/LSP. EN: semantic integration'
  default_prompt: 'Используй $serena-lsp-integration: свяжи Serena с LSP. EN: semantic LSP setup.'
dependencies:
  tools:
  - type: mcp
    value: serena
    description: 'Семантика Serena. EN: code symbols, refs, memory.'
policy:
  allow_implicit_invocation: true
