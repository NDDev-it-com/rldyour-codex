interface:
  display_name: Serena Code Workflow
  short_description: 'Код через Serena. EN: semantic code work'
  default_prompt: 'Используй $serena-code-workflow: изучи код через Serena. EN: semantic code flow.'
dependencies:
  tools:
  - type: mcp
    value: serena
    description: 'Семантика Serena. EN: code symbols, refs, memory.'
policy:
  allow_implicit_invocation: true
