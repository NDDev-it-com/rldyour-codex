interface:
  display_name: Verification Gates
  short_description: 'Проверка quality gates. EN: verification commands'
  default_prompt: 'Используй $verification-quality-gates: запусти проверки. EN: quality gates.'
dependencies:
  tools:
  - type: mcp
    value: serena
    description: 'Семантика Serena. EN: code symbols, refs, memory.'
  - type: mcp
    value: playwright
    description: 'Браузерные flows. EN: screenshots and assertions.'
policy:
  allow_implicit_invocation: true
