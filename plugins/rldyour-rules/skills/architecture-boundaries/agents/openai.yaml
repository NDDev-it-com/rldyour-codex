interface:
  display_name: "Architecture Boundaries"
  short_description: "Apply FSD and VSA boundaries"
  default_prompt: "Use $architecture-boundaries to verify placement, imports, slices, and backend use-case structure."

dependencies:
  tools:
    - type: "mcp"
      value: "serena"
      description: "Semantic architecture and reference inspection."
    - type: "mcp"
      value: "context7"
      description: "Official docs for architecture-related framework constraints."

policy:
  allow_implicit_invocation: true

