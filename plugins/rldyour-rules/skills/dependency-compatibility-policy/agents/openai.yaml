interface:
  display_name: "Dependency Compatibility"
  short_description: "Use latest compatible dependencies"
  default_prompt: "Use $dependency-compatibility-policy to check versions, compatibility, release notes, and lockfiles."

dependencies:
  tools:
    - type: "mcp"
      value: "context7"
      description: "Official dependency and framework documentation."
    - type: "mcp"
      value: "grep"
      description: "Real-world dependency usage patterns."

policy:
  allow_implicit_invocation: true

